# CLAUDE.md（个人项目版）

> 本文件是 Claude Code / AI 编程助手在**我个人项目**中的工作规范。
> 个人项目讲究"轻流程、重纪律"：没有公司的 CI / 审批 / 运维兜底，所以**每一行代码都得自己对自己负责**。流程可以砍，工程判断不能砍。
>
> **分工**：本文件管"AI 怎么工作 + 规范没覆盖的翻车点"；同目录的 `next开发规范.md` / `python开发规范.md` 管"代码长什么样"。

---

## 执行优先级

冲突时，序号小的优先：

1. 本文件 `CLAUDE.md`
2. 同目录 `next开发规范.md`
3. 同目录 `python开发规范.md`
4. 项目内其他说明文档（README 等）
5. 项目现有代码结构、目录约定、命名方式和实现风格

> 个人项目通常把这几个文件放在仓库根目录或 `docs/` 下，跟代码同仓维护，不依赖外部规范仓库。

---

## 技术栈形态（动手前必须确认）

本仓库同时存在 `next开发规范.md` 与 `python开发规范.md`，但**一个具体项目通常只走其中一种形态**。开工前必须先确定属于哪种，未声明时 **AI 必须先问，禁止假设**：

- **纯 Next 全栈**：前后端都在 Next，API 走 `src/app/api/`，只读 `next开发规范.md`。
- **Next 前端 + Python 后端分离**：前端读 `next开发规范.md`，后端读 `python开发规范.md`，**两端共享同一套接口契约**（见下方「接口与契约」）。
- **纯 Python 服务 / 脚本**：只读 `python开发规范.md`。

> 在 README 顶部用一行声明当前项目形态（例：`> 形态：Next 前端 + Flask 后端分离`），省得每次重新确认。

---

## 核心原则

1. **正确性 > 速度 > 重构完美度**
2. **每次改动必须：可解释、可验证、可回滚**
3. **先理解需求，再改代码**
4. **先复用，再抽象，最后才新增**
5. **先说明方案，再执行修改**（个人项目里，"我"既是产品也是 reviewer，方案说清楚是说给未来的自己听）
6. **最小改动优先**，避免无关重构
7. **不确定时先问清楚**，不擅自假设
8. **AI 生成代码默认不可信**，必须显式验证边界

### 心法四条（Karpathy）

上面 8 条是规则，下面 4 条是心法。遇到规则没覆盖的情况，按心法判断：

1. **Think Before Coding** —— 不要偷偷做假设；存在多种合理解读时全部列出让我选，不要静悄悄选一种就往前跑；看到更简单的路，主动 push back；看不懂就停下来说清楚哪里看不懂
2. **Simplicity First** —— 只写解决问题的最小代码；不写没被要求的"灵活性 / 可配置性"；不给不可能的分支写 error handling；不对单次使用的代码做抽象。**自检：一个资深工程师会不会说"过度设计"？** 200 行能做的事写了 1000 行，重写
3. **Surgical Changes** —— 只动必须动的地方；不顺手"美化"相邻代码/注释/格式；**每一行 diff 都要能追溯到原始需求**；看到无关的 dead code 只提醒、不动手
4. **Goal-Driven Execution** —— 把命令式任务（"加校验"）翻译成可验证目标（"先写失败用例，再让它通过"）；多步任务先列 "步骤 → 验证方式" 再动手

> 个人项目最容易翻车的就是 Simplicity First：一个人写代码没人拦着过度设计，AI 又特别爱"贴心地"加配置项和抽象层。明确反对这种行为。

---

## 动手前确认（按改动大小分级）

个人项目不必每次都走全套流程，但**也不能省掉"理解 + 看现有代码"这一步**。

### 写任何代码之前，先做两件事

- **查看现有代码**：相关目录、已有组件、工具函数、接口定义、数据结构。能复用就别新写。
- **确认改动范围**：这次到底要碰哪些文件、不碰哪些。

### 改动分级

- **琐碎改动**（改文案、修 typo、调样式、加一行日志）：直接做，做完一句话说清改了什么。
- **一般改动 / 新功能 / 任何涉及数据或接口的改动**：先用 **3 句话确认**，等我确认后再动手：
  1. **需求是什么** —— 一句话描述要解决的问题
  2. **准备怎么改** —— 涉及哪些模块/文件/接口，以及**不涉及**哪些
  3. **风险在哪里** —— 可能的副作用、兼容性问题、容易踩的坑
- **结构性改动**（调目录、改公共模块、动数据库 schema、引入新依赖）：必须停下来确认，不得自作主张。

### 遇到歧义和不合理方案时

- **存在多种合理解读**：全部列出，说明各自取舍，让我选。**禁止静默选一种然后开写。**
- **我的方案有更简单的替代**：先提出来并说明理由，再等我决定。不要只因为"我说了"就去实现明显绕弯的方案。
- **不理解某处业务逻辑 / 现有代码**：停下来点明不理解的是什么，不要猜着写。

---

## 需要确认才能做的事

**通用操作禁令以本节为单一来源**（下方列表）。`next开发规范.md` / `python开发规范.md` 的"禁止操作清单"只补充各自语言/框架特有项（Prisma / Alembic migration、`next.config.ts`、虚拟环境等），不重复本节通用项——避免同一条禁令散落三处、改一处漏两处。个人项目里这些尤其要守，因为没有 CI 和 code review 兜底。

**通用禁令 + 个人项目专属高危项**：

- ❌ **绝不把密钥、密码、token、连接串写进任何会进 git 的文件**（包括 `.env`、源码、注释、commit message）。个人仓库一旦 push 到公开平台，泄露不可逆。密钥统一进被 gitignore 的 `.env`，模板进 `.env.example`。
- ❌ **不要未经确认就 `git commit` / `git push`**。即使是个人项目，提交前我要先看 diff。暂存变更、说清改了什么，等我确认。
- ❌ **绝不 `git push --force` / `git reset --hard` / 删历史**。
- ❌ **不要删除文件、目录或大段代码**（除非是本次改动制造的孤儿代码，见下文）。
- ❌ 不要为了"看起来更干净"做无关重构。
- ❌ 不要擅自修改 `CLAUDE.md`、`next开发规范.md`、`python开发规范.md`。
- ❌ 不要升级已锁定的依赖版本（见各语言规范的版本约束）。
- ❌ 不要全局安装 npm / pip 包，一律装到项目本地。

**以下情况必须停下来确认**：

- 需要调整目录结构
- 需要修改公共组件或底层基础设施
- 需要引入新依赖（先说装什么、为什么、有没有更轻的替代）
- 需求描述不清，存在多种实现路径
- 发现规范与当前代码现状冲突

---

## 代码修改要求

> 通用技术栈、命名风格、Tailwind、Server Components、类型注解、zod/pydantic 校验、日志格式等详见 `next开发规范.md` 与 `python开发规范.md`。
> 本章节只列规范未覆盖的常见翻车点。

### 命名与类型

- 命名必须表达业务语义。禁止 `temp`、`data2`、`handleX`、`doSomething`、`utils` 这类模糊名。
- 类型不确定时先用 `unknown` 收窄后再使用，禁止滥用 `any`。
- 魔法数字、魔法字符串必须提取为命名常量。
- 枚举值和状态码使用 TypeScript `enum` / `as const` 联合类型，或 Python `Enum`，禁止裸字符串散落各处。

### 输入输出契约

- **所有函数/接口的输出必须有稳定结构**，不允许有时返回对象、有时返回 `null`、有时抛异常。
- 接口错误必须返回明确的错误码 + 用户可读信息。
- 必填字段缺失返回 **4xx**，不允许返回 500。

### 写操作

- **写操作必须处理失败分支，禁止静默失败。**
- 禁止 `try { ... } catch(e) {}` 或 `catch(e) { console.log(e) }` 然后继续往下走。
- 重要写操作必须**幂等**：同一请求重复提交不能产生脏数据。
- 多步写入必须使用事务或提供补偿机制。

### 异步逻辑（必须同时考虑）

- **竞态**：后发请求先返回如何处理（请求 id 判断或 AbortController）。
- **重复点击**：按钮必须有 loading 态或防抖。
- **超时**：所有网络请求必须设置 timeout。
- **取消**：组件卸载、路由切换时必须能中断。

### 权限校验

> 即使是个人项目，只要有"多用户"或"对外暴露"的可能，越权就是真问题。自己用的纯本地脚本可酌情放宽，但**任何对外可访问的接口都要校验**。

- 权限 = **"是否登录"** + **"是否有权操作该资源"**，两者都要校验。
- 任何 `/api/xxx/:id`、`/api/xxx?id=` 类接口，**必须**校验资源归属当前用户。
- 前端隐藏按钮 ≠ 后端不校验。前后端都要做，以后端为准。
- **越权是一票否决项。**

---

## 状态与错误处理

### 四态模型（强制）

所有涉及异步/远程调用的流程，至少要有 4 种状态：

```
idle  →  loading  →  success
              ↓
            error
```

- **禁止只有 loading 和 success 两态。**
- **空数据必须有明确的空状态**，不允许白屏或只显示 loading。
- **error 态必须给可重试的入口**，重试不产生脏数据。

### 错误分层

- **给用户看的**：可理解的人话 + 下一步动作建议。
- **给日志看的**：完整技术细节（堆栈、请求参数、响应内容、时间戳）。
- 两者不能混用。

---

## 接口与契约

- 前后端契约**单一来源**（共享 types 包 / OpenAPI / 直接同仓共享类型）。
- 字段名、枚举值、状态码前后端必须完全一致（别前端 `userName`、后端 `user_name`）。
- 状态机流转必须**显式校验当前状态是否合法**。
- 禁止隐式副作用（默认广播、默认全局修改、默认跨模块写入）。

### 统一响应结构（单一来源）

无论 Next API 路由还是 Python 后端，对外 JSON 响应**用同一套封装**（前端只写一份解析逻辑）。错误响应也是 JSON，HTTP 状态码与业务码并存、不可矛盾（业务码用于前端精细化处理，HTTP 码用于通用判断）：

```jsonc
// 成功：HTTP 200
{ "code": 0,     "data": { /* ... */ }, "msg": "ok" }
// 失败：HTTP 4xx/5xx，code 为业务错误码
{ "code": 40001, "data": null,          "msg": "参数错误" }
```

> `next开发规范.md` / `python开发规范.md` 只放各自的实现示例，**结构定义以本节为准**，改契约只改这里。

---

## 可维护性

- 同一段逻辑出现**第 2 次**时，就考虑抽取公共函数/组件。（注意是第 2 次，不是第 1 次就预先抽象。）
- 公共能力放稳定边界（`lib/`、`utils/`、`shared/`），业务逻辑放业务层。
- 注释解释**"为什么这么做"**，不解释**"这行代码做了什么"**。
- 一次提交只做一件事，重构和功能开发不能混合提交。

### 死代码的两种情况（区分处理）

- **本次改动让某段代码变成死代码（你制造的 orphan）**：直接删除，不要只注释掉。包括本次改动造成的无用 import、变量、函数。
- **与本次改动无关的、本来就存在的死代码**：**只提醒、不动手。** 在汇报里单独列"顺带发现的死代码：xxx"，等我决定是否单独清理。

---

## 任务目标化（Goal-Driven Execution）

把命令式指令翻译成可验证的目标，再开干：

| 原始指令 | 翻译为可验证目标 |
|---------|----------------|
| "加一个校验" | "为非法输入写失败用例，让它通过" |
| "修这个 bug" | "先写一个能复现 bug 的测试，再让它通过" |
| "重构 X" | "重构前后所有现有测试保持通过" |
| "让它能跑" | ❌ 拒绝，要求给出具体成功标准 |

超过 2 步的任务，动手前先列出 "步骤 → 验证方式"：

```
1. [步骤] → 验证方式：[怎么知道这步完成]
2. [步骤] → 验证方式：[怎么知道这步完成]
```

需求模糊到无法翻译成可验证目标（"让它更好"、"优化一下"）时，回到确认环节让我给出具体标准，不要自己脑补。

---

## 测试要求（最小集）

> 个人项目不强求覆盖率数字，但**关键链路必须有可重复运行的验证**——哪怕是一段手动验证步骤，也要写清楚。

每个关键改动尽量覆盖 4 种路径：

1. **成功路径**：输入合法、权限合法、依赖正常。
2. **参数错误路径**：缺失、类型错误、越界。
3. **权限/越权路径**：未登录、已登录但无权限、跨用户访问（如果项目有多用户）。
4. **异常/失败路径**：依赖服务失败、网络超时、数据库错误。

如果本次改动没补测试或测不了，**必须在汇报里显式写出缺口**，不要假装测过了。

---

## 合并/收尾前检查（DoD）

### 一票否决项（出现任一项即不算完成）

1. ❌ 越权漏洞未关闭（如项目有多用户）
2. ❌ 关键写操作无错误处理
3. ❌ 关键参数未校验直接使用
4. ❌ 静默吞错（`catch` 了但不处理、不上报、不日志）
5. ❌ 只验证了 happy path，没验证失败路径
6. ❌ 前端能操作但后端必报错的字段/状态不一致问题
7. ❌ 打印了敏感信息到日志
8. ❌ 密钥/密码进了会被 git 追踪的文件

### 正向检查清单（逐项打勾或显式说明缺口）

- [ ] 功能主路径通过
- [ ] 边界条件和失败态通过
- [ ] 权限与数据归属校验通过（如适用）
- [ ] 无竞态和重复提交问题
- [ ] 前后端契约一致（字段名、枚举值、状态码）
- [ ] 有对应测试或明确的手动验证步骤，或明确说明测试缺口
- [ ] **每行 diff 都能追溯到本次需求**（无顺手重构、无无关改动）
- [ ] `.env` 等敏感文件已 gitignore，未把密钥带进提交
- [ ] 变更说明清楚：改了什么、为什么改、如何验证

---

## Vibe Coding 专属守则

### "先快写，后收口"的正确顺序

允许先用 AI 快速生成原型代码，但**收尾前必须按以下顺序收口**（顺序不能乱——先收类型是因为类型一旦收紧，很多潜在问题会自动暴露）：

1. **收口类型** —— 消除所有 `any`，把 `unknown` 收窄到具体类型。
2. **收口错误处理** —— 补全所有异步调用的 error 分支。
3. **收口权限** —— 补全资源归属校验（如适用）。
4. **收口测试** —— 至少补齐成功路径 + 失败路径（+ 权限路径，如适用）。

> 个人项目的最大陷阱：原型"跑起来了"就以为做完了。原型只是第一步，未收口的代码不算完成。

### AI 代码的默认怀疑清单

AI 自己生成代码后，必须主动自查以下高频翻车点：

- 是不是写了 `try { ... } catch(e) { console.log(e) }` 然后继续往下走？
- 是不是接口里只校验了登录，忘了校验资源归属？
- 是不是只写了 loading 和 success，没写 error 和空状态？
- 是不是异步逻辑没考虑组件卸载后还在 setState？
- 是不是魔法字符串散落在代码各处？
- 是不是有些字段前端叫 `userName`、后端叫 `user_name`？
- **是不是顺手改了跟本次需求无关的代码、注释或格式？**
- **是不是加了我没要的"灵活性"、抽象层或配置项？**
- **是不是有 dead code 是本次改动造成的（该删）还是本来就存在的（不该碰）？**
- **是不是在某个文件里硬编码了密钥或把它写进了 `.env` 之外、会进 git 的地方？**

**任何"看起来能跑"的代码，在未通过 DoD 前都不算完成。**

---

## 标准输出格式

### 开始任务时（一般及以上改动）

1. **需求理解** —— 我理解你要做的是……
2. **修改方案** —— 准备这样改……（如有多种合理解读，全部列出）
3. **预计影响文件** —— 会涉及……
4. **验证标准** —— 做完后用什么方式验证算完成……
5. **风险点或需要确认的地方** —— 需要你确认……
6. **开始修改**（确认后）

### 任务完成后

**本次修改**
- 修改文件：
- 改动概要：

**影响范围**
- 受影响页面/模块：
- 是否影响接口/数据结构：

**DoD 自检**
- [ ] 主路径 / 边界 / 权限 / 契约 / 测试 / diff 可追溯 / 无密钥泄露

**验证方式**
- 本地验证步骤：
- 建议重点检查项：

**顺带发现（不动手，只提醒）**
- 无关死代码 / 潜在问题 / 可优化点：

**待确认事项**
- 是否需要继续执行提交/补充优化：

---

*个人项目版。本规范是活文档，遇到新的翻车模式随时补充。*
